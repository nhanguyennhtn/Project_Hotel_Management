import { useLocation } from "react-router-dom";


export default function contractDetail() {
    // const contractDetail = useLocation().state
    return (
        <div>
            {contractDetail.fullname}
        </div>
    )
}